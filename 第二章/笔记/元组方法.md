``````python
"""元组方法
    与列表一样，唯一区别是元组不能修改。
    通常使用圆括号包裹元组，当然也可以不包裹。


    # 元组声明方法 #
    将一些值用逗号分割就能自动创建元组。


    # 单个元素元组的创建 #
    创建单个元素元组时需要在元素后面加上逗号，如下
    >>> 32,
    (32,)
    如果不加逗号就会变成数字32
    >>> 32
    32


    # 列表与元组转换 #
    tuple([1, 2, 3]) -> (1, 2, 3)
    list((1, 2, 3)) -> [1, 2, 3]
    
"""
``````
